services:
  infer:
    build: .
    image: timesformer-infer
    volumes:
      - ${LAB6_INPUT_DIR:?Set LAB6_INPUT_DIR to a folder with exactly 8 images}:/app/input:ro
    command: ["--input", "/app/input"]
