services:
  infer:
    build: .
    image: timesformer-infer
    volumes:
      - ./weights:/app/weights:ro
      - ./sample_input:/app/input:ro
    command: ["python", "-m", "src.predict_frames", "--input", "/app/input"]
